cd C:\Users\YASH.SHAH\Downloads\BillingSystem
git rm railway.toml
git commit -m "remove railway.toml"
git push
```

**Step 2 — Go to Railway website** → Your API service → Click **Settings** tab → Find **Deploy** section → Find **Start Command** field → Type this exactly:
```
dotnet BillingSystem.API.dll